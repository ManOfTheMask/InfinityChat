<!--generate a box with a button to create a new key pair and have a script to turn the private key into a file download-->
<div class="flex flex-col items-center justify-center w-screen h-screen relative"> 
    <div class="bg-base-200 shadow-lg p-8 rounded-lg text-center w-7/10 h-2/3 "> 
        <h1 class="text-base-content text-4xl mb-4">Generate PGP Key Pair</h1> 
        <div class="mb-4">
            <input type="text" id="username" class="input input-bordered w-full" placeholder="Username (for PGP key)" autocomplete="off">
        </div>
        <div class="mb-4">
            <input type="password" id="passphrase" class="input input-bordered w-full" placeholder="Passphrase (for private key)" autocomplete="new-password">
        </div>
        <p class="text-base-content text-lg mb-4">Click the button below to generate a new PGP key pair.</p> 
        <button id="generateKeyButton" class="btn btn-primary">Generate Key Pair</button>
        <div class="w-full h-5/10 overflow-y-auto p-4 flex flex-col items-center" >
            <button id="downloadPrivateKeyBtn" class="btn btn-secondary mt-2" style="display:none;">Download Private Key</button>
            <a id="privateKeyDownloadLink" style="display:none;"></a>
            <pre id="keyPairOutput" class="mt-4"></pre> 
            <div id="errorMessage" class="text-red-500 mt-2"></div>
        </div>
    </div> 
</div> 
